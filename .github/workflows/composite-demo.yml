name: Composite Action Demo

on:
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run Composite Action
        uses: ./.github/actions/setup-and-greet

        with:
          name: Josh
          language: en

      - run: echo "Composite action completed!"
